/** @type{import('prettier').Config} */
const config = {
  arrowParens: "always",
  bracketSpacing: true,
  printWidth: 80,
  semi: false,
  trailingComma: "none",
  singleQuote: true,
  useTabs: false,
  tabWidth: 2,
  endOfLine: "lf",
  plugins: [],
};

export default config;
